// TODO: Unified timeline synchronization hook
// - Sync video capture timestamps with Rerun timeline
// - Handle DataChannel frame timestamp relay
// - Expose: currentTime, isLive, seek(), setRate()

export function useTimeSync() {
  // placeholder
}
