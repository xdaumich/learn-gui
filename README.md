# learn-gui

## Environment setup (uv)

```bash
uv venv .venv
source .venv/bin/activate
uv pip install -r requirements.txt
```

## External dependencies

This repo tracks source dependencies as git submodules under
`external_dependencies/`.
